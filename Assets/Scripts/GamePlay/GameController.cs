using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour
{
    //opponent score
    //player score
    //match time

    private void Awake() {
        // onplacement.createNewPlayer += TrackPlayer(p)
    }

    //if player fail to pass -> score -1
    //if player goal -> score +1
    //onmatch time = 0 -> score 0 -> new match 
}
